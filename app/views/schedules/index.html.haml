%h1 Scheduled Matches for Teams you Follow

.schedule.span-12.prepend-2.append-2
  - for (scheduled_date, match_info) in @scheduled_dates.sort{|a, b| a.first <=> b.first }
    - if scheduled_date >= Date.today
      .match-date.span-12.last
        %h2{ :style=>'display:block;' }= scheduled_date.strftime('%B %d, %Y')

        - for match in match_info.sort{|a, b| a[:time] <=> b[:time] }
          .match-info
            .span-3.prepend-1
              = match[:time].strftime('%I:%M %p')
            .span-6
              = match[:field]
            .span-10.last
              = match[:match]
%a.advertisement.vertical{ :href => @advertisement.url, :style=>'text-decoration:none; font-weight:bold; border: 1px solid;' }
  - unless @advertisement.title.nil?
    %h2= @advertisement.title
  - unless @advertisement.logo.nil?
    = image_tag @advertisement.logo.url
  - unless @advertisement.description.nil?
    %p= @advertisement.description
